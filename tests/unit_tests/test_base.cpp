#include <gtest/gtest.h>

TEST(InitialTest, CheckTestFunctionality) {
    // Making sure that the test framework is working correctly
    EXPECT_EQ(0, 0);
}

/*
    FOR WRITING NEW TESTS WITH GTEST PLEASE put any helper functions in the anonymous namespace at the top of the file
*/
